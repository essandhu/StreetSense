# views/

Compositions of components into screens. Lives one layer above
`components/`: a view is a small file that wires queries, state, and
components together for one route or one major UI surface.

Phase 1 ships one view: `MapView` — the map page mounted at `/`.
