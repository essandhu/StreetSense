# scoring/propagator/src/CMakeLists.txt
#
# Phase 4.1.7 placeholder. Real targets (the propagator engine + the
# StrategyRegistry + the first concrete strategy) land in Phase 4.2.
#
# Refs:
#   - conductor/tracks/phase-4-propagator/plan.md Phase 4.2
#   - scoring/propagator/include/streetsense/propagator/ (public headers)

# No targets at this phase. The umbrella project (one level up) skips
# linkage to this directory's outputs until Phase 4.2 fills it in.
